import { makeStyles } from '@material-ui/core/styles';

export const useStyles = makeStyles(() => ({
  promoCode: {
    margin: '10px 0'
  },
  promoButton: {
    height: '48px',
    marginRight: 15
  }
}));

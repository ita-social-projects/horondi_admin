import { makeStyles } from '@material-ui/styles';

export const useStyles = makeStyles(() => ({
  container: {
    margin: '30px 0',
    display: 'flex',
    direction: 'row',
    justify: 'flex-start',
    alignItems: 'center',
    spacing: 2
  }
}));

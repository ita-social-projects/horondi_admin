import { makeStyles } from '@material-ui/core/styles';

export const useStyles = makeStyles((theme) => ({
  red: {
    fontWeight: 'bold',
    color: 'red'
  },
  blue: {
    fontWeight: 'bold',
    color: 'blue'
  },
  green: {
    fontWeight: 'bold',
    color: 'green'
  }
}));

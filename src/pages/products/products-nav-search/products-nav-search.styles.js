import { makeStyles } from '@material-ui/core/styles';

export const useStyles = makeStyles((theme) => ({
  clearButton: {
    height: '36px'
  },
  root: {
    padding: '2px 4px',
    display: 'flex',
    alignItems: 'center',
    width: 200,
    height: '36px'
  },
  iconButton: {
    padding: 10
  },
  container: {
    width: '365px'
  }
}));

// import { createSelector } from 'reselect';
// import { selectPagination } from '../table/table.reducer';
// import { selectSidePockets } from '../side-pockets/side-pockets.reducer';

// export const sidePocketsSelector = createSelector(
//   selectSidePockets,
//   (sidePocket) => sidePocket
// );

// export const sidePocketsSelectorPagination= createSelector(
//   selectSidePockets,
//   selectPagination,
//   (sidePocket, table) => ({
//     ...sidePocket,
//     ...table
//   })
// );
